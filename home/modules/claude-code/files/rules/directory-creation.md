# Directory & File Creation

- NEVER use `mkdir` or `touch` via Bash. Always use the Write tool instead
- Write auto-creates parent directories and can create empty files
- Only exception: `.keep` files for directories needed before a non-Write tool writes to them
