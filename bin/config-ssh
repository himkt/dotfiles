#!/bin/bash

export WINPATH=`wslpath "$(wslvar USERPROFILE)"`
gcloud compute config-ssh --ssh-key-file=~/.ssh/id_rsa
cp $HOME/.ssh/config $WINPATH/.ssh

echo Launch vim and delete IdentitiesOnly and add User
echo $EDITOR $WINPATH/.ssh/config
